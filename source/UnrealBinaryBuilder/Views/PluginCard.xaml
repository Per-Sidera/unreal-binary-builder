<UserControl x:Class="UnrealBinaryBuilder.Views.PluginCard"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:vm="clr-namespace:UnrealBinaryBuilder.ViewModels"
			 mc:Ignorable="d"
			 d:DataContext="{d:DesignInstance Type=vm:PluginCardViewModel}"
			 Width="260">
	<Border Background="{DynamicResource RegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12">
		<StackPanel>
			<TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
			<TextBlock Text="{Binding EngineVersionLabel}" Foreground="{DynamicResource PrimaryBrush}" Margin="0,2,0,8" FontSize="11"/>
			<TextBlock Text="{Binding Description}" TextWrapping="Wrap" Opacity="0.75" FontSize="11" MaxHeight="60"/>
			<Border Margin="0,10,0,0" CornerRadius="4" Padding="6,3" HorizontalAlignment="Left">
				<Border.Style>
					<Style TargetType="Border">
						<Setter Property="Background" Value="DimGray"/>
						<Style.Triggers>
							<DataTrigger Binding="{Binding State}" Value="Building">
								<Setter Property="Background" Value="DodgerBlue"/>
							</DataTrigger>
							<DataTrigger Binding="{Binding State}" Value="Succeeded">
								<Setter Property="Background" Value="ForestGreen"/>
							</DataTrigger>
							<DataTrigger Binding="{Binding State}" Value="Failed">
								<Setter Property="Background" Value="Crimson"/>
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Border.Style>
				<TextBlock Text="{Binding State}" Foreground="White" FontSize="11"/>
			</Border>
		</StackPanel>
	</Border>
</UserControl>
