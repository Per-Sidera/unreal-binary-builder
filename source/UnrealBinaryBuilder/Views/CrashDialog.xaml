<hc:GlowWindow x:Class="UnrealBinaryBuilder.Views.CrashDialog"
			   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			   xmlns:hc="https://handyorg.github.io/handycontrol"
			   mc:Ignorable="d"
			   Title="Unhandled exception"
			   Background="{DynamicResource RegionBrush}"
			   ActiveGlowColor="{DynamicResource PrimaryColor}"
			   InactiveGlowColor="{DynamicResource SecondaryBorderColor}"
			   Width="800" Height="540"
			   WindowStartupLocation="CenterOwner">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0" Text="Something went wrong." FontSize="18" FontWeight="SemiBold"/>
		<TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,4,0,12"
				   Text="An unhandled exception was caught. Copy the trace below and open an issue if it keeps happening."/>
		<hc:TextBox Grid.Row="2" x:Name="TraceText" IsReadOnly="True" FontFamily="Consolas" FontSize="11"
					AcceptsReturn="True" TextWrapping="NoWrap"
					HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/>
		<StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
			<Button Content="Copy" Width="120" Margin="0,0,8,0" Click="OnCopy"/>
			<Button Content="Close" Width="100" Click="OnClose"/>
		</StackPanel>
	</Grid>
</hc:GlowWindow>
